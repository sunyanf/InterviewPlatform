# AI 面试模拟平台 · Agent 宪法

> 版本：v1.0.0  
> 级别：仓库最高级 AI Coding 规则  
> 语言：中文  
> 适用范围：整个仓库，以及所有参与本仓库开发的 AI Coding Agent

---

## 1. 文件定位

本文件是本仓库的 **Agent 最高级规则**。

任何 AI Coding Agent 在：

- 阅读代码
- 分析任务
- 修改代码
- 修改数据库
- 修改 Prompt
- 修改 Agent
- 修改 RAG
- 修改测试
- 创建 PR

之前，必须读取并遵守本文件。

本文件不是产品需求文档，也不是技术教程。

它定义的是：

> **AI 在这个项目里“必须怎么工作、不能怎么工作，以及什么事情必须交给人决定”。**

---

# 2. 最高原则

本项目遵循：

> **人负责目标和关键决策，Agent 负责分析与实现，代码和数据库负责运行时事实。**

进一步说：

```text
人
├── 为什么做
├── 做什么
├── 产品边界
├── 重大架构
├── 业务规则
└── 不可逆决策

Agent
├── 阅读上下文
├── 设计实现方案
├── 编写代码
├── 编写测试
├── 运行验证
├── Review
└── 提出风险

代码 / 数据库
├── 状态事实
├── 权限事实
├── 事务事实
├── 时间事实
└── 最终业务结果
```

---

# 3. 规则优先级

发生冲突时，按照以下优先级：

```text
P0  平台安全规则
P1  用户 / 人工明确指令
P2  根目录 AGENTS.md
P3  当前目录及父目录更近的 AGENTS.md
P4  ADR / 架构决策
P5  Feature Spec / Task / Contract
P6  当前代码与测试
P7  一般工程惯例
P8  Agent 自行推断
```

越靠前优先级越高。

---

# 4. 近目录规则优先

如果修改：

```text
backend/internal/agent/
```

Agent 应读取：

```text
/AGENTS.md
/backend/AGENTS.md
/backend/internal/agent/AGENTS.md
```

后面的规则可以对前面的规则进行**更具体的补充**，但不得违反更高层的安全和项目原则。

---

# 5. Agent 启动协议

任何任务开始时，严格执行：

```text
1. 读取根 AGENTS.md
2. 搜索目标目录附近的 AGENTS.md
3. 读取 docs/PROJECT_MEMORY.md
4. 读取相关架构文档
5. 读取相关模块说明
6. 读取任务文件 / Feature Spec
7. 搜索相关代码
8. 搜索相关测试
9. 判断影响范围
10. 输出任务理解
11. 设计最小实施方案
12. 再开始修改
```

禁止：

> 未读取上下文就直接改代码。

---

# 6. 任务理解协议

编码前至少明确：

```text
目标
背景
输入
输出
影响范围
非目标
约束
相关模块
相关测试
潜在风险
```

复杂任务必须先输出：

```text
## 我的理解
...

## 相关代码
...

## 实施方案
...

## 风险
...

## 验证方案
...
```

---

# 7. 最小修改原则

Agent 必须遵循：

> **完成目标所需的最小变更原则。**

禁止“顺手”：

- 重写模块
- 更换框架
- 更换 ORM
- 升级大量依赖
- 改公共 API
- 修改无关数据库表
- 重命名大量目录
- 把单体强行拆成微服务

如果确实需要扩大范围：

1. 说明原因；
2. 说明影响；
3. 记录决策；
4. 高风险变化需要人工批准。

---

# 8. 绝对禁止事项

未经人工明确批准，不得：

```text
1. 删除生产数据
2. 删除核心数据库表
3. 修改生产环境
4. 提交密码、Token、密钥
5. 绕过认证或授权
6. 绕过安全校验
7. 删除测试以使测试通过
8. 修改测试断言以掩盖代码问题
9. 修改业务规则以适配模型输出
10. 让 LLM 直接控制核心状态
11. 大规模重构无关模块
12. 自动改变架构决策
13. 修改 ADR 而不说明原因
14. 引入没有必要的基础设施
15. 将用户声明直接当作已验证事实
```

---

# 9. AI 系统最高原则

本项目是 AI 应用系统。

必须区分：

## 确定性逻辑：代码负责

```text
认证
授权
Session 状态
面试计时
权限
事务
最终分数计算
幂等
重试
限流
资源配额
数据库写入
```

## 非确定性理解：模型负责

```text
自然语言理解
问题生成
回答分析
事实提取
能力分析
总结
学习建议
```

原则：

> **LLM 可以提出意见，但不能成为系统最终事实来源。**

---

# 10. Session 状态只能由业务代码控制

例如：

```text
INIT
READY
RUNNING
PAUSED
FINISHING
EVALUATING
COMPLETED
FAILED
```

不能因为模型说：

> “用户已经完成面试。”

就直接把状态改成 `COMPLETED`。

必须：

```text
Go Service
→ 校验业务条件
→ 修改状态
→ 写数据库
```

---

# 11. LLM 输出必须经过验证

凡是代码消费的 LLM 输出，优先：

```text
Structured Output
↓
Schema Validation
↓
Business Validation
↓
Normalization
↓
业务处理
```

禁止：

```text
LLM 字符串
↓
直接 JSON Parse
↓
直接写数据库
```

---

# 12. LLM Provider 必须抽象

业务模块不能直接绑定单一模型厂商。

应采用：

```text
业务模块
   ↓
项目内部 LLM Interface
   ↓
Provider Adapter
   ├── Provider A
   ├── Provider B
   └── Local Model
```

---

# 13. Prompt 是代码

Prompt 必须：

```text
版本化
可追踪
可回滚
可测试
```

Prompt 版本发生变化时：

- 更新版本号；
- 记录修改原因；
- 说明预期影响；
- 必要时重新运行 Eval Dataset。

---

# 14. RAG 来源必须可追踪

知识库必须保留：

```text
source
source_url
source_type
collected_at
version
```

禁止把来源不明的信息标记为：

```text
官方标准
权威规则
当前政策
真实题库
```

---

# 15. Evaluation 必须基于证据

正确：

```text
问题
+
用户回答
+
Rubric
+
参考知识
+
Evidence
↓
维度评分
↓
最终评分
```

错误：

```text
LLM：
“你表现不错，所以 78 分。”
```

最终总分尽可能由确定性代码计算：

```text
维度分 × 权重 → 总分
```

---

# 16. 公务员 / 考试类知识必须有时间和来源

涉及：

- 政策
- 法律
- 时政
- 考试规则
- 评分规则

必须保存：

```text
source
effective_from
effective_to
version
```

过期内容不能默认作为当前标准。

---

# 17. 用户数据与隐私

可能涉及：

```text
简历
工作经历
语音
图片
联系信息
学习记录
```

原则：

> 最小化采集、最小化访问、最小化日志。

禁止日志输出：

```text
密码
Token
完整身份证号
完整手机号
完整邮箱
完整简历
完整音频内容
```

---

# 18. 数据库变更

数据库结构必须通过 Migration 管理。

流程：

```text
设计
↓
migration
↓
测试
↓
Review
↓
执行
```

破坏性迁移：

> 必须人工确认。

---

# 19. API 规则

统一：

```text
/api/v1/...
```

API 必须有：

```text
稳定语义
统一错误格式
权限边界
幂等语义
```

---

# 20. Go 规则

优先：

```text
简单
清晰
可测试
可观测
可维护
```

优先使用标准库。

不要为了“架构高级”制造没有实际价值的抽象。

---

# 21. Context 规则

以下 IO 必须正确传递 `context.Context`：

```text
数据库
Redis
HTTP
LLM
文件
对象存储
RPC
```

请求链路中不得随意使用：

```go
context.Background()
```

替换已有上下文。

---

# 22. Goroutine 规则

每个新增 goroutine 必须回答：

```text
谁创建？
谁停止？
什么时候退出？
失败怎么办？
是否可能泄漏？
```

优先：

```text
context
errgroup
bounded worker pool
```

---

# 23. 测试规则

每个新功能至少覆盖：

```text
正常路径
边界条件
异常路径
```

AI 功能额外覆盖：

```text
模型输出为空
字段缺失
字段类型错误
模型输出不符合业务规则
上下文缺失
RAG 无结果
LLM 超时
LLM 重试
```

---

# 24. 不得讨好测试

测试失败后，不得直接：

```text
修改测试期望
删除测试
关闭断言
```

必须先判断：

```text
代码错？
测试错？
需求变了？
```

然后修复真实原因。

---

# 25. Agent 什么时候必须停止

遇到以下情况，必须停止并请求人工决策：

```text
1. 重大需求歧义
2. 核心业务规则冲突
3. ADR 冲突
4. 破坏性数据库修改
5. 公共 API 不兼容变更
6. 核心架构变更
7. 新增核心基础设施
8. 高风险安全问题
9. 大规模重构
```

汇报：

```text
问题
原因
候选方案
风险
推荐方案
需要人工决定的事项
```

---

# 26. Done 定义

任务只有同时满足以下条件才算完成：

```text
□ 功能完成
□ 架构边界正确
□ 单元测试
□ 必要的集成测试
□ 错误路径
□ 日志 / Trace / Metrics
□ 文档同步
□ 安全检查
□ 无无意义 TODO
□ gofmt
□ lint / vet
□ 测试通过
□ Git diff 检查
□ 已生成 Git commit
```

---

# 27. Agent 最终汇报格式

```markdown
## 完成情况

### 已完成
- ...

### 修改文件
- ...

### 核心设计
- ...

### 测试
- ...

### 验证结果
- ...

### 风险
- ...

### 未完成
- ...

### 后续建议
- ...
```

禁止只说：

> “已完成。”

---

# 28. Git 提交规范

## 28.1 每完成一个任务或功能必须生成 commit

规则：

```text
每完成一个 Task / 功能 / 修复 / 重构，
必须立即生成一个 Git commit，
然后再开始下一个任务。
```

目的：

- 保持提交粒度清晰，每个 commit 对应一个完整变更；
- 便于回溯、回滚和 Code Review；
- 避免大量无关变更堆积在一个 commit 中。

要求：

1. 一个 Task 完成后，先验证（测试 / vet / diff），再 commit；
2. commit 只包含该 Task 相关的变更，不要混入无关修改；
3. 多个独立 Task 不要合并成一个 commit；
4. 若 Task 内有多个文件，统一在一个 commit 中提交；
5. commit message 遵循 28.2 规范。

## 28.2 Commit Message 规范

推荐：

```text
feat: 增加面试 session
fix: 修复追问状态问题
test: 增加评分边界测试
refactor: 提取 LLM Provider
docs: 更新 Agent 架构
```

格式：

```text
<type>: <description>

[optional body]
```

type 范围：

```text
feat      新功能
fix       修复 bug
test      测试
refactor  重构
docs      文档
chore     构建 / 工具 / 依赖
perf      性能
```

---

# 29. Agent 的最终行为准则

如果只记十句话：

1. **先读上下文，再写代码。**
2. **先理解，再修改。**
3. **最小修改，不顺手重构。**
4. **LLM 不拥有系统最终状态。**
5. **确定性问题交给代码。**
6. **AI 输出必须验证。**
7. **Prompt 和模型必须版本化。**
8. **代码、测试、文档必须同步。**
9. **高风险变化必须人工决策。**
10. **完成不是代码写完，而是验证完成。**
